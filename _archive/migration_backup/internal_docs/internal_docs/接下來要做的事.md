# 接下來要做的事

---

## 必須做（讓一切回到 main、網站正常）

您還在分支 `update-project-overview-20260205`，且這支分支比 **main** 多「文件結構與內部文件」的更新。要讓 GitHub 上的 main 與網站一致，請：

1. **關閉** Cursor 裡有開的檔案（或改用**本機 PowerShell** 在專案目錄操作）。
2. 在終端機依序執行：

```powershell
cd "C:\github\SL_Client_Case\HER _CHYUN"
git checkout main
git pull origin main
git merge update-project-overview-20260205 -m "merge: 合併文件結構與流程更新"
git push origin main
```

3. 完成後您就會在 **main** 上，之後改 `docs/` 只要 **commit → push origin main**，網站會自動更新。

---

## 建議做（本機殘留變更）

- **01_Discovery、02_Analysis** 裡有 7 個 .md 已修改未提交：若要保留就 `git add` 後在 main 上 commit；不要就 `git restore <檔案>` 還原。
- **docs/** 根目錄下的 `coding_standard_program_codes.md`、`協作與治理規範.md` 若與 `internal_docs/` 重複，可刪除避免混淆。

---

## 可選

- 若不再需要舊分支，可刪除：  
  `git branch -d update-project-overview-20260205`
- 本說明檔（`接下來要做的事.md`）可加入版本控制：  
  `git add internal_docs/接下來要做的事.md` 後一併 commit。

---

**一句話**：先完成「必須做」的 4 行指令，之後就只要在 main 上改文件並 push 即可。
